import org.scalatest.funsuite.AnyFunSuite
import org.scalatest._
import seroglazov.org.FTest._

class FTests extends AnyFunSuite {
    test("FTest contains item") {
      val ftest: FSet[Int] = FSet(3)
    }
}
